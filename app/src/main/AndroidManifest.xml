<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Signature-level permission must be declared as a direct child of <manifest> -->
    <permission
        android:name="fi.iki.pnr.kioskhelper.permission.KIOSK_CONTROL"
        android:protectionLevel="signature"
        android:label="@string/perm_label"
        android:description="@string/perm_description" />

    <!-- Package visibility for Android 11+ so we can resolve Signal's launcher -->
    <queries>
        <!-- See any app with a launcher activity -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- And explicitly the Signal package used by our fork -->
        <package android:name="org.thoughtcrime.securesms" />
    </queries>

    <application
        android:label="Kiosk Helper"
        android:allowBackup="false"
        android:supportsRtl="true">
        <receiver
            android:name=".AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".KioskCommandActivity"
            android:exported="true"
            android:permission="fi.iki.pnr.kioskhelper.permission.KIOSK_CONTROL"
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="fi.iki.pnr.kioskhelper.ACTION_ENABLE_KIOSK" />
                <action android:name="fi.iki.pnr.kioskhelper.ACTION_DISABLE_KIOSK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
